install:
  - cp tests/options.docker_compose_example.yml tests/options.yml
  - docker build -t python_libstorj -f ./dockerfiles/python_libstorj.dockerfile .
script:
  - "echo 'TODO: run user creation script'"
  - docker run python_libstorj bash -c "./build.sh && python -m unittest discover"
env:
  - STORJ_BRIDGE="http://bridge:6382"
  - STORJ_EMAIL="username@example.com"
  - STORJ_PASS="password"
  - STORJ_MNEMONIC="abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon abandon about"
