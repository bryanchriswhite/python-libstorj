version: "2"
services:
  python_libstorj:
    build: .
    links:
      - bridge
    volumes:
      - ./lib:/python_libstorj/lib
      - ./tests:/python_libstorj/tests
  bridge:
    image: storjlabs/storj-integration
    environment:
      STORJ_BRIDGE: http://bridge:6382
      STORJ_KEYPASS:
    ports:
     - 6382:6382
    command: bash -c ". /root/.nvm/nvm.sh && ./scripts/start_everything.sh && /bin/sleep infinity"
